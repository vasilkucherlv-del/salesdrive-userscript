# 📋 Вижимка для перенесення в іншу сесію

## Проєкт
- **Репозиторій:** `vasilkucherlv-del/salesdrive-userscript`
- **Файл:** `salesdrive.user.js` — Tampermonkey-юзерскрипт для CRM SalesDrive (`komplektom.salesdrive.me`)
- **Гілка/пуш:** усі зміни — у `main`. Файл **паралельно редагують і на GitHub**, тож перед пушем завжди `git fetch origin main && git rebase origin/main`.
- **Поточна версія:** **1.41** (`// @version` піднімається з кожним комітом)
- **Перевірка перед пушем:** `node --check salesdrive.user.js`
- **Автооновлення:** у шапці є `@updateURL`/`@downloadURL` на raw GitHub (raw кешується ~5 хв). Щоб оновити у Tampermonkey: Утиліти → «Перевірити оновлення».

## Структура файлу — модулі в рамках
Кожен top-level модуль обгорнуто рамкою:
```
/* ▼▼▼ МОДУЛЬ-START • <id> — опис ▼▼▼ */
... код ...
/* ▲▲▲ МОДУЛЬ-END • <id> ▲▲▲ */
```
Угорі файлу є **TOC (карта модулів)**. Правити код модуля ТІЛЬКИ в межах його рамок.
**Конвенція:** кожен новий модуль так само обгортати в START…END із унікальним `id`.

Модулі (зверху вниз):
- **core** — ядро: шина подій (`BUS`), дані з Google-таблиць, стилі; містить `content.js` (підказки/ціни/рейтинг/ТТН) і Базу знань.
- **lkNaboryInline** — позначка «входить у набори» в рядках заявки.
- **lkAnalogInline** — інлайн-значок «🔁 аналог» у рядку товару.
- **lkModalKits** — рядок «Входить у набори» в картці товару (модалка).
- **lkUpsellRedesign** — компактний вигляд картки допродажу.
- **lkStockPayWarn** — попередження: передоплата + малий залишок.
- **lkCashRegister** — 💰 Каса самовивозу.
- **lkAnalogStyles** — стилі банера «Аналоги».
- **lkQuickPickup** — ➕ швидка кнопка нової заявки самовивозу.
- **lkAutoOrgByPayment** — автопідстановка організації під вхідний платіж.

---

## Готові функції (ключове)

### Аналоги (інлайн)
- Таблиця Google Sheets: ID `1S9DN1lNw7wanmJSHCOY9l3dQ9plSBnEPGHWreK_p20c` (відкрита на читання). Колонки: `0=код якоря,1=назва якоря,2=код аналога,3=назва аналога,4=примітка`. Змінна `ANALOG_SHEET_ID`.
- Значок «🔁 аналог» у рядку товару (праворуч від «+» комплектів); клік розгортає аналог + «➕ Додати».
- Карта `buildAnalogBySku` **двобічна** — значок на обох товарах пари. Старий бірюзовий банер вимкнено.

### Каса самовивозу (`lkCashRegister`)
- `баланс = опорний_залишок + готівкові_продажі − видатки` (від дати коригування).
- При «⚙️ Скоригувати залишок» зберігається **опорний залишок на початок дня** = `введена_сума − сьогоднішні_продажі + сьогоднішні_видатки` (фікс подвійного рахунку; не залежить від часу оплати).
- `amount(o)` = `paymentAmount`, якщо заповнено, інакше `|restPay|` (бо `restPay` буває відʼємним). ⚠️ НЕ використовувати `paymentAmount − restPay` (зануляє все).
- Константи: `CASH_ID=44`, `CARD_ID=100`, `STATUS_ID=5`. Baseline на `BARCODE_URL` (`barcode-printer-production-2b32.up.railway.app`) через `/api/cash-baseline`.

### Автопідстановка організації (`lkAutoOrgByPayment`)
- Якщо в заявці є **вхідний платіж** (`a[href*="incoming-payment"]`) → читає ФОП із запису платежу → виставляє ту саму **організацію** в полі `div.stylized-select[attr-field-name="organizationId"]` (select2) → відправник підтягується сам.
- Вибір організації — синтетичним кліком по полю + клік потрібного `li.select2-results__option`. **ВАЖЛИВО:** `new MouseEvent` БЕЗ `view:window` (у пісочниці Tampermonkey `window` — Proxy, інакше TypeError).
- Після підстановки поле позначене 🔒. Клік питає «Точно потрібна ручна зміна?» → при «ОК» вимикає автозаміну для заявки (`override`) і відкриває вибір.
- Прапорець `DEBUG` (типово `false`) вмикає логи `[SD-Орг]` у консоль.

---

## ⚠️ Технічні нюанси середовища
- **Проксі блокує `salesdrive.me`** — з агента не можна напряму смикати API CRM (403 CONNECT). Перевірка лише через скрипт у браузері менеджера (консоль F12, фільтр за тегом).
- Файл редагують паралельно на GitHub → завжди rebase перед пушем, версія вже може бути вищою.

## Відкрите / можливі наступні кроки
- Каса: за бажанням розширити фільтр (зараз лише статус «Оплачено САМОВИВІЗ» = 5).
- Автоорг: `override` діє в межах перегляду заявки (після перезавантаження правило застосовується знову).
